services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: chat_demo
      POSTGRES_PASSWORD: chat_demo
      POSTGRES_DB: chat_demo
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
